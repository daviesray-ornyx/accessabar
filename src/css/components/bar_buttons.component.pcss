#accessabar .bar-button {
    margin: 0.15em;
    outline: none;
    border: none;
    background: var(--dark-grey);
    color: var(--font-colour);
    padding: 0.5em;

    &:hover, &:focus {
        background: var(--lighter-grey);
        cursor: pointer;
        outline: 2px solid var(--blue-focus);
    }

    &.close {
        background: var(--dark-red);

        &:hover, &:focus {
            background: var(--lighter-red);
        }
    }
}
