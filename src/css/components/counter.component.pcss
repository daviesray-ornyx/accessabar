#accessabar .counter {
    margin: 0.5em 0;
    justify-content: center;
    align-items: center;

    .inc, .dec {
        margin: 0 0.5em;
        padding: 1em;
    }

    .count {
        margin: 0 0.5em;
        font-size: 1.8em;
    }
}
